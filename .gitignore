# 依赖
node_modules/

# electron-vite 构建产物
out/
*.tsbuildinfo

# electron-builder 打包产物
# 注意：build/ 不在此列，它用于存放图标等打包资源
dist/
release/

# 环境变量与本地配置
.env
.env.local
.env.*.local
.claude/settings.local.json

# 编辑器与 IDE
.vscode/
.idea/
*.swp
*.swo

# 操作系统
.DS_Store
Thumbs.db
desktop.ini

# 日志
*.log
logs/

# spike 的运行产物（脚本本身保留，便于在其它机器上复核）
spike/report.json
spike/*.png
spike/out/

# 评审截图：是过程证据，不是产物，体积也大
.impeccable/review/
