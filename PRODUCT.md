# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

（界面由 Electron 内的 Chromium 渲染，是 HTML/CSS/JS 的 Web 表面；应用本身是 Windows 桌面程序。）

## Users

主要用户是在办公室电脑前工作的人，想在上班时间读小说、刷题或浏览内容，而不被同事、主管或屏幕共享看到。他们的处境是：屏幕随时可能被人瞥见，不能关掉工作窗口，也常常只有很窄的一段时间可以分神。

用户要完成的任务：在**不离开工作界面的前提下**，低风险地持续阅读一段内容。

## Product Purpose

把网页装进一个可调透明度、置顶、能一键藏起来的窗口里，让「在办公场景下阅读」这件事在视觉上不成立。

成功意味着：用户敢于把它一直开着，而不是每次打开都担心被看到；且被看到的瞬间有明确的、来得及的应对手段。

## Positioning

与普通浏览器或阅读器不同，本产品的核心机制是**「被看见的成本」而非「阅读的效率」**：

- 窗口本身可以逐像素透明，与桌面融为一体；
- 光标移出后主体区域自动隐藏，并且**该区域的鼠标事件穿透到后方窗口**——用户可以在原位置继续操作 Word，就像那里什么都没有；
- 全局老板键与托盘提供了失焦状态下的应急手段。

相邻产品可以照抄「透明窗口」，但「隐藏即穿透、且不影响正常工作流」才是这个产品真正的机制。

## Operating Context

- **Windows 桌面，办公环境。** 用户的屏幕上同时有工作软件；窗口需要与它们共存，而不是独占屏幕。
- **随时可能被打断。** 应对手段必须在对方走进视线的几秒内完成，且不能产生声音、焦点抢占或任务栏变化。
- **屏幕共享是真实威胁。** 远程会议中共享屏幕的情形比「有人走过来」更高频。
- **开发者/项目背景：** 项目是开源的（GPL-2.0-or-later），功能对标商业产品墨鱼阅读，但代码与素材完全独立实现。仓库内已有真机验证结论（`docs/spike-findings.md`）。

## Capabilities and Constraints

**已实现（里程碑 1）：** 无边框透明置顶窗口、无极透明度、鼠标移出收起成悬浮球、老板键、托盘、内嵌浏览器（多标签、我的站点、热门站点、历史、书签、手机/电脑模式、缩放、隐藏滚动条、持久化登录态；起始页与系统设置各有自己的进出口，不占标签条）、起始页按栏目分节（全部 / 视频 / 阅读 / 资讯 / 刷题 / 离线阅读，栏目归属由内置站点表按注册域判定，认不出的域名只出现在「全部」里）、离线阅读（「打开文件…」弹系统选文件框，选中的 TXT 与 PDF 直接开成标签页：TXT 交给 Chromium 自己渲染，**PDF 由自家那一页排**——pdf.js 画进画布再把纸键掉，桌面透得过纸，字随主题换成墨色，彩色明写为代价；本机文件在界面上只显示文件名，从不显示路径）、**阅读透明度**（右栏第三条滑块，0%–100%：只淡**正在读的正文**——本机 TXT 与自家 PDF 阅读页，界面与网页都不受影响，于是「界面 100% + 正文 40%」这种搭配成立；没有正在读的本机文件时它是禁用的）、系统设置、主题（纸白 / 暗夜 / 磷绿，管整个界面）、自动检查更新（启动后静默查一次 + 设置页手动查，提示只出现在窗口内）。

**尚未实现：** EPUB / MOBI / AZW3 的阅读（Chromium 不认这三种格式，选中会变成一次下载，因此选文件框里不列它们）、PDF 的排版控件（字号、行距、页边距）与链接 / 目录 / 选中文字（那一页现在只有纸与字）、自动滚动、修改网页文字颜色、按站点插件。这些在路线上，界面设计应为其预留位置而非假装它们已存在。

**技术约束：**
- Electron 44 + Vue 3 + TypeScript；`src/renderer` 是渲染进程。
- **chrome 层覆盖整个窗口，中部必须完全透明**——它下方叠着网页视图，网页隐藏时露出的是桌面。任何一层不透明底色都会破坏这个机制。
- **不能使用 `backdrop-filter` / 毛玻璃**：它在透明窗口背后模糊不了桌面，只能模糊窗口自身内容，效果是错的。
- 窗口默认 960×540（16:9），可自由调整；另有四档尺寸预设。隐蔽形态是**收起成悬浮球**：窗口真的缩到 40×40，屏幕上不留看不见却仍接收点击的区域。
- 主题管的是整个界面——顶栏、地址栏、标签条、右侧栏、悬浮球、弹出面板、系统设置页，连起始页一起换；**网页本身永远不受影响**，这一条是硬边界。终端形态（磷绿的直角与等宽字体）同样只作用于界面。
- **阅读透明度也只作用于本机文件**（`file:`，见 `isLocalFile`）：它淡的是「被读的正文」，而网页那一侧永远吃不到——与上一条同一条边界，判据由主进程给，界面只按同一个谓词决定那条滑块活不活得起来。
- **更新提示只出现在窗口内，不用系统通知。** 系统通知会进通知中心、会上锁屏、会被录屏录进去——那等于替用户把「我在跑别的程序」说出去。更新同样不自作主张，但**只问两次**：下载由用户发起（点了「检查更新」才开始下，那一次 110MB 以上；启动时的静默检查只提示、不下），装与重启由「更新并重启」那一下就交出去——静默安装、装回上次那个目录、装完把新版自己启动回来，中间不再有向导。更新源只有 HTTPS 与 `latest.yml` 里的 sha512 兜底，**没有代码签名**，SmartScreen 仍可能提示。
- 界面语言为简体中文。

**明确未决：** 起始页条目的人工排序与个性化规则未定（当前次序是「我的站点 → 常访问 → 内置热门站点」，同一个域名只出现一次）；栏目表在更窄窗宽下是否需要第三档字号未实测。

## Brand Commitments

- 名称：**摸鱼阅读**（英文标识 `moyu-reader`）。
- 项目在仓库 README 中的既有语气：直接、坦白、不吹嘘，愿意把限制和失败模式写明（例如明确写出「GPL 允许他人商用」、明确写出注册失败会提示）。
- 许可证 GPL-2.0-or-later 是既成事实，界面中的「关于」需如实呈现。
- 不做任何超出已实现功能的宣称。

## Evidence on Hand

**真实可用：** 应用本身（可运行、可截图）；`src/renderer` 中的现有界面；`docs/spike-findings.md` 的真机验证结论；`src/shared/presets.ts` 中的内置站点列表（阅读：微信读书、晋江文学城、起点中文网、番茄小说、京东读书、QQ 阅读；资讯：知乎、豆瓣、小红书；视频：哔哩哔哩、抖音、爱奇艺、优酷、快手、YouTube；刷题：粉笔、牛客网、力扣）。

**不存在，且不得虚构：** 用户量、评价、媒体报道、客户案例、价格与会员体系、任何性能或安全承诺的量化指标。

## Product Principles

1. **隐蔽优先于美观。** 任何视觉选择若提高「被一眼认出是阅读工具」的概率，都应让位——包括品牌露出的强度。
2. **不干扰工作流。** 隐藏后不得吞掉本该落在后方软件的点击；不得抢焦点；不得在任务栏与 Alt+Tab 中留下痕迹。
3. **状态要可解释。** 用户必须始终能判断当前处于什么状态（透明中/已隐藏/在托盘），并且总能找回窗口。宁可少一个功能，也不能出现「窗口不见了」。
4. **坦白而非美化。** 失败要如实上报（如快捷键注册失败），限制要写明（如某功能尚未实现），不用界面掩盖产品的真实边界。
5. **熟悉胜过新奇。** 这是 Operate 型工具界面，用户的注意力在内容上而不在软件上；交互应遵循桌面浏览器与 Windows 的既有习惯。

## Accessibility & Inclusion

- 界面需在低透明度（25%–45%）下仍可辨认，此时文字与背景的对比度会整体下降——因此关键控件的可点击区域不能只靠颜色区分，需要形状或位置上的区分。
- 用户可能长时间注视，字号不应过小；正文与关键标签不得低于 12px。
- 不使用仅靠颜色传达的状态（如仅用红/绿表示进程或失败）。
