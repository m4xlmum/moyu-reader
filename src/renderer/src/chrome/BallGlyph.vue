<script setup lang="ts">
/**
 * 悬浮球的图标集。
 *
 * 与 `Icon.vue`（界面自己的图标）分开，因为两者的尺度和用途都不同：
 * 那一套画在 15px 的按钮里，这一套画在 40px 的球面上——球面上只有 18px 画东西，
 * 那点地方认得出来靠的是外轮廓，不是笔画。因此这里全部是**只取剪影**的图形，
 * 不去凑 `Icon.vue` 那套更细的线。
 *
 * 线条语言与 `Icon.vue` 一致：24 视框、圆头圆角、只用 currentColor。
 * 描边略粗（1.7 而不是 1.6）是因为球比按钮大，球上的线也应当跟着粗一点。
 *
 * 图形用一张表 + `v-for` 出 `<path>`，而不是每个图标一段 `<template v-if>`：
 * 加一个图标是加一行数据，不是加一段模板。
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */
import { BALL_GLYPH_SIZE } from '@shared/constants'
import type { BallIcon } from '@shared/constants'

withDefaults(
  defineProps<{
    name: BallIcon
    /** 图形边长（像素）。不传就是球面上那个尺寸——设置页的预览要的正是它 */
    size?: number
  }>(),
  { size: BALL_GLYPH_SIZE }
)

/** 每个图标的若干段路径。表里的 id 必须与 @shared/constants 的 BALL_ICONS 一一对应 */
const GLYPHS: Record<BallIcon, string[]> = {
  // 摊开的书。原本就写死在 Ball.vue 里，抽出来当第一枚
  book: [
    'M4 5.5h6a2.5 2.5 0 0 1 2 2.5v11a2 2 0 0 0-1.6-1.4H4z',
    'M20 5.5h-6a2.5 2.5 0 0 0-2 2.5v11a2 2 0 0 1 1.6-1.4H20z'
  ],
  // 半睁的眼：上下两条眼睑 + 一枚瞳孔
  eye: [
    'M2.8 12.2C5.1 8 8.3 5.9 12 5.9s6.9 2.1 9.2 6.3',
    'M2.8 12.2C5.1 16.4 8.3 18.5 12 18.5s6.9-2.1 9.2-6.3',
    'M14.6 12.2a2.6 2.6 0 1 1-5.2 0 2.6 2.6 0 0 1 5.2 0'
  ],
  // 一条鱼：身子、尾鳍、一枚眼睛
  //
  // 身子是一个正椭圆（两段半圆拼出来），尾巴是一枚明显比身子高的楔形，
  // 尖端**插进身子**里 0.4 个单位——18 像素上那点缝根本看不见，看起来就是长在一起的。
  // 第一版把尾巴画得比身子矮，放大看是一枚小楔子挂在左边，缩回 18px 就成了一块糊掉的边。
  // 眼睛离身子那条边留了 1.8 个单位：再近就会跟轮廓连成一笔，鱼脸就成了个钩子。
  fish: [
    'M20.6 12a7.7 4.8 0 1 0-15.4 0 7.7 4.8 0 1 0 15.4 0',
    'M5.6 12 1.8 7.6v8.8z',
    'M16.8 10.5a1.2 1.2 0 1 0-2.4 0 1.2 1.2 0 1 0 2.4 0'
  ],
  // 一杯热咖啡：杯身、手柄、两缕热气
  coffee: [
    'M4.2 9.6h10.6v5.8a3.4 3.4 0 0 1-3.4 3.4H7.6a3.4 3.4 0 0 1-3.4-3.4z',
    'M14.8 10.8h1.5a2.3 2.3 0 0 1 0 4.6h-1.5',
    'M7.5 6.6V3.2',
    'M11.2 6.6V3.2'
  ],
  // 一弯月亮。两段 8.6 半径的弧拼成一枚月牙（一段小弧挖、一段大弧兜）
  moon: ['M20.5 14.4A8.6 8.6 0 0 1 9.6 3.5a8.6 8.6 0 1 0 10.9 10.9z'],
  // 猫：带两只尖耳的猫脸 + 两只闭着的笑眼
  //
  // 耳朵不是两枚独立的三角，而是**长在脸的轮廓上**：整张脸一笔画完，
  // 四个转折点全落在同一个半径 6 的圆上（脸心 12,13），因此耳朵与脸严丝合缝。
  // 第一版是「圆脸 + 两个悬在头顶的三角」，悬空的那点缝在 18px 上是一圈糊，
  // 放大看更明显：那两枚三角跟脸根本没有关系。
  // 第二版把耳根收在头顶正中（±20°），结果两只耳朵在头顶挤成一撮、朝里倒，
  // 像兔子——耳根改到 ±25° 与 ±55°，耳朵就朝外张开，是猫的样子了。
  cat: [
    'M5.07 4.73L9.46 7.56A6 6 0 0 1 14.54 7.56L18.93 4.73L16.91 9.56A6 6 0 1 1 7.09 9.56Z',
    'M9.1 12.5c.5.7 1.4.7 1.9 0',
    'M13 12.5c.5.7 1.4.7 1.9 0'
  ],
  // 尖括号与斜杠
  code: ['M9.2 7.4 4.6 12l4.6 4.6', 'M14.8 7.4 19.4 12l-4.6 4.6', 'M13.2 5.4 10.8 18.6'],
  // 一副耳机：头梁与两只耳罩
  headphones: [
    'M4.4 14.4v-2.6a7.6 7.6 0 0 1 15.2 0v2.6',
    'M6 13.8H5.2a1 1 0 0 0-1 1v3.2a1 1 0 0 0 1 1H6a1.6 1.6 0 0 0 1.6-1.6v-2A1.6 1.6 0 0 0 6 13.8z',
    'M18 13.8h.8a1 1 0 0 1 1 1v3.2a1 1 0 0 1-1 1H18a1.6 1.6 0 0 1-1.6-1.6v-2A1.6 1.6 0 0 1 18 13.8z'
  ]
}
</script>

<template>
  <svg
    class="ball-glyph"
    :width="size"
    :height="size"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="1.7"
    stroke-linecap="round"
    stroke-linejoin="round"
    aria-hidden="true"
  >
    <path v-for="(d, i) in GLYPHS[name]" :key="i" :d="d" />
  </svg>
</template>

<style scoped>
.ball-glyph {
  display: block;
  flex: 0 0 auto;
}
</style>
